<%= form_for(@message_post) do |f| %>
  <% if @message_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message_post.errors.count, "error") %> prohibited this message_post from being saved:</h2>

      <ul>
      <% @message_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :thread_id %><br />
    <%= f.number_field :thread_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
